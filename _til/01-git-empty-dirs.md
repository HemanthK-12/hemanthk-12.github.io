---
cat: gotcha
title: "git doesn't track empty directories"
---
add a `.gitkeep` file inside to force git to include the folder. warna it'll just ignore the folder and doesn't even show up in untracked files.
